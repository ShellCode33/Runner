#include <iostream>
#include "window.h"

using namespace std;

int main(int argc, char **argv)
{
    if(argc > 1)
    {
        //easter egg
    }

    WindowRunner window;
    window.create();

    return 0;
}
